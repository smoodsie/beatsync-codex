# Beatport Credentials (optional - only needed for authenticated API access)
# If you have a Beatport account, you can provide credentials here
# to potentially unlock additional API access for full playlist extraction
BEATPORT_EMAIL=your-email@example.com
BEATPORT_PASSWORD=your-password
